"""vrgrid — adaptive variable-resolution 2.5D LiDAR mapping (SIH26053).

This package is the FROZEN shared surface: `cell` and `api`. Implementations
live in `vrgrid.grid`, `vrgrid.gpu`, `vrgrid.perception`, `vrgrid.eval`.
"""

__version__ = "0.0.0"
